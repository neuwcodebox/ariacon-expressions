interface:
  display_name: "Ariacon Expressions"
  short_description: "Default Ariacon reaction images"
  default_prompt: "Use $ariacon-expressions to add a fitting Ariacon image by default unless the response should stay plain."

policy:
  allow_implicit_invocation: true
